## 2026-08-23T20:54:00Z
You are the Release Worker for Peidagogos STEAM.

CRITICAL INSTRUCTIONS:
1. Follow PowerShell git rules strictly: Run `git add`, `git commit`, and `git push origin master` as SEPARATE commands (never use `&&` or chaining operators).
2. Working directory: `d:\Peidagogos_Oficial`.
3. Check `git status`.
4. Stage all changes with `git add -A` or `git add .`.
5. Commit with a descriptive message: `git commit -m "feat(director-grupo): implement Director de Grupo module with R1-R5, tab routing, Montenegro teacher management, student registration link generator, and adversarial tests"`.
6. Push to origin master: `git push origin master`.
7. Verify git status after push.
8. Deliver a handoff report documenting the git execution results.
